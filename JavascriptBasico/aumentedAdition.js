/**
 * Todo lo que esta a la derecha de la asignacion se evalua primero
 * Podemos usar operador += para agregar mas cantidad en vez de ++
 * 
 * Convierte las asignaciones de a, b y c para que utilicen el operador +=.
 */

let a = 3;
let b = 17;
let c = 12;

a += 12;
b += 9;
c += 7;