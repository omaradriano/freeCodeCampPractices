/**Dividir sobre un numero aumentado 
 * 
 * Convierte las tareas de a, b, y c para utilizar el operador /=.
*/

let a = 48;
let b = 108;
let c = 33;

// Cambia solo el código debajo de esta línea
a /= 12;
b /= 4;
c /= 11;